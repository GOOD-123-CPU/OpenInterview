# 安全策略

## 支持的版本

| 版本 | 支持状态 |
|------|----------|
| 3.0.x | ✅ 安全修复 |
| 2.0.x | ⚠️ 仅重大漏洞 |
| < 2.0 | ❌ 不再维护 |

## 报告漏洞

**请勿在公开 Issue / PR / 讨论区中披露安全漏洞。**

请使用 GitHub 的 **Private vulnerability reporting** 功能（仓库 → Security → Report a vulnerability）私密上报。

报告请包含：

1. 漏洞类型与影响面（如：未授权访问 X 接口）
2. 复现步骤或 PoC
3. 影响评估（数据泄露？权限绕过？）
4. 若有修复建议，欢迎一并提供

## 处理流程（SLA）

1. **48 小时内**确认收到
2. **7 天内**给出初步评估（是否成立、严重级别 CVSS）
3. 修复发布后**公开致谢**（除非你希望匿名）

| 严重级别 | 目标修复时长 |
|----------|--------------|
| Critical | ≤ 7 天 |
| High | ≤ 14 天 |
| Medium | ≤ 30 天 |
| Low | 下个常规版本 |

## 安全设计要点（供审计参考）

- 管理端口令：PBKDF2-HMAC-SHA256，260,000 迭代，随机盐
- 会话令牌：HMAC-SHA256 签名，`hmac.compare_digest` 时序安全比较
- 登录限流：5 次/分/IP（内存滑动窗口）
- SQL：全参数化，无字符串拼接
- 候选人端凭据：32 位随机 token（`secrets` 模块）
- 敏感配置：环境变量注入，`.env`/证书目录均在 `.gitignore`
- 依赖：Dependabot 每周扫描；CI 集成 detect-secrets

## 已知设计边界（透明披露）

- 候选人面试链接持有者即可作答（无个人身份验证）——设计如此，链接应通过私密渠道分发
- 限流器为进程内存实现，多实例部署需换 Redis 等集中式方案
- 无状态令牌无法服务端主动吊销单会话（`logout` 为客户端语义）
